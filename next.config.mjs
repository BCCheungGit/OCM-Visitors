/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.planningcenteronline.com', "store.cloudority.com"],
    }
};

export default nextConfig;
