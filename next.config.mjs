/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.planningcenteronline.com', "image.cloudority.com"],
    }
};

export default nextConfig;
